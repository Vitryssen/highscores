# Public values only: the publishable key is safe to ship in the frontend (RLS protects data).
# Copy to .env.local for local development. Never put the secret/service-role key here.
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_KEY=sb_publishable_...
